import { ResetPasswordForm } from "../../../components/auth-form";

export const metadata = { title: "Choose a password | OmniLede Contributor" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
